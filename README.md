# Traceroute-on-Windows
Windows UDP Traceroute implemented in C# using raw sockets. Sends UDP probes with increasing TTL, listens for ICMP Time Exceeded / Destination Unreachable messages, and supports optional reverse DNS lookup
